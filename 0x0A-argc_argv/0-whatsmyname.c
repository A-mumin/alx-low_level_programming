#include <stdlib.h>
#include <stdio.h>

/**
 * main - entry point
 *
 * @Return: always zero
 */

int main(int agrc, char* argv[])
{
	if (agrc == 0)
	{
		printf ("\n");
	}
	printf ("%s\n", argv[0]);
	return (0);
}
