#include "main.h"
/**
 * print_alphabet_x10 - To print 10 times the alphabet, in lowercase, followed by a new line
 *
 * Return: Always zero 
 */
void print_alphabet_x10(void)
{
	char lc;
	int i;

	for (i = '1'; i <= '9'; i++)
	{
		for (lc = 'a'; lc <= 'z'; lc++)
		{
			putchar(lc,);

		}

	putchar('\n');

	}
}
