#include "main.h"
#include <stdio.h>
/**
 * add - Adds two integers and returns the result
 * @a: integer
 * @b: another integer
 * Return: returns an integer
 */
int add(int a, int b)
{
	return (a + b);
}

