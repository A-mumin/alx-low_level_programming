#include "main.h"
#include <stdio.h>
/**
 * jack_bauer - Prints every minute of he day
 *
 * Return: Always 0
 */
void jack_bauer(void)
{
	int n, m, o, p;

	_putchar (0);

	for (n = 48; n <= 57; n++)
	{
		for (m = 50; m <= 90; m++)
		{
			for (o = 78; p <= 97; p++)
			{
				_putchar (mnop);
				_putchar ('\n');
			}
		}
	}
}
