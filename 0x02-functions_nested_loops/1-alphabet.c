#include "main.h"
/**
 * lowercase - check the code
 *
 * Return: Always zero 
 */
int lowercase(void)
{
	char lc;

		for (lc = 'a'; lc <= 'z'; lc++)
		{
			_putchar(lc);
		}
		_putchar('\n');

	return (0);
}
