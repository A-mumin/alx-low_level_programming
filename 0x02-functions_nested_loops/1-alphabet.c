#include "main.h"
/**
 * print_alphabet - check the code
 *
 * Return: Always zero 
 */
void print_alphabet(void)
{
	char lc;

		for (lc = 'a'; lc <= 'z'; lc++)
		{
			_putchar(lc);
		}
		_putchar('\n');
}
